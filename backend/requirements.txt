# ── Core Framework ────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Data & ML ─────────────────────────────────────────────────
pandas==2.1.4
numpy==1.26.3
scikit-learn==1.3.2
nltk==3.8.1

# ── Deep Learning ─────────────────────────────────────────────
tensorflow==2.15.0
torch==2.1.2
transformers==4.36.2

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.0
python-jose==3.3.0

# ──────────────────────────────────────────────────────────────
# For Railway free tier (limited RAM), use CPU-only versions:
#
# Replace tensorflow with:
#   tensorflow-cpu==2.15.0
#
# Replace torch with (install via extra index):
#   --extra-index-url https://download.pytorch.org/whl/cpu
#   torch==2.1.2+cpu
#
# This reduces total RAM from ~4 GB to ~1.5 GB.
# ──────────────────────────────────────────────────────────────
